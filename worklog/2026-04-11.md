# Worklog — 2026-04-11

## Tổng kết ngày
Hoàn thành Phase 1 core: user-service + api-gateway + common library restructure.

---

## Actions thực hiện

### Common Library
- Restructure 4 module cũ → 2 module: `common-entity` + `common-base`
- Implement design patterns: Strategy (CacheStrategy, DistributedLock, RateLimiter), Observer (EventPublisher, EventConsumer, DomainEvent), Builder (ApiResponse), Template Method (CrudService), Adapter (EntityMapper)
- Tạo `META-INF/spring/AutoConfiguration.imports` để Spring Boot auto-load configs
- Update `CLAUDE.md` với full documentation cấu trúc common

### API Gateway
- Thêm Swagger/OpenAPI aggregation — proxy routes `/v3/api-docs/{service}`
- Config `swagger-ui.urls` để aggregate docs từ 7 services
- Fix port conflict: Kafka UI `8080` → `9000`

### Docker Compose
- Thêm: MongoDB, Elasticsearch, RabbitMQ, Zipkin
- Thêm UIs: Redis Insight, Mongo Express, Kafka UI
- Kafka dual listeners: `PLAINTEXT_HOST://localhost:9092` + `PLAINTEXT://kafka:29092`
- Tạo init scripts: `scripts/postgres/init-multiple-dbs.sh`, `scripts/mongodb/init.js`

### User Service — Exceptions Fix
- Fix 4 exception classes dùng sai constructor `TicketingException`
- `EmailAlreadyExistsException` → `HttpStatus.CONFLICT`
- `InvalidCredentialsException` → `HttpStatus.UNAUTHORIZED`
- `InvalidTokenException` → `HttpStatus.BAD_REQUEST`
- `UserNotFoundException` → `HttpStatus.NOT_FOUND`

### Git
- Commit tất cả thay đổi
- Tạo PR lên GitHub

---

## Bugs fixed
- Common modules không install vào `~/.m2` → chạy `mvn install -pl common/...`
- `mvnw` không tìm thấy → dùng system `mvn`
