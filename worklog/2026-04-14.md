# Worklog — 2026-04-14

## Tổng kết ngày
Hoàn thành toàn bộ `event-catalog-service` (port 8082) — 14 tasks theo plan. Branch `feature/event-catalog-service` đã được push lên remote, PR tạo thủ công trên GitHub.

---

## Actions thực hiện

### event-catalog-service — Hoàn thành (Tasks 12–14)

**Task 12: Kafka Consumers**
- Tạo `infrastructure/messaging/TicketStatusConsumer.java`
  - `@KafkaListener` cho 3 topics: `ticket.reserved` → LOCKED, `ticket.released` → AVAILABLE, `ticket.issued` → SOLD
  - `.subscribe()` hợp lệ tại Kafka entry point (non-reactive boundary)
- Tạo `infrastructure/messaging/MediaUploadedConsumer.java`
  - Skeleton: log payload, TODO comment cho khi `media-service` được build

**Task 13: Cache Integration + EventPublisher Bean**
- Tạo `infrastructure/config/CatalogConfig.java`
  - `EventPublisher<Object>` bean → `KafkaEventPublisher<>(kafkaTemplate, "event-catalog-service")`
  - `RedisCacheStrategy<EventResponse>` bean với `ReactiveRedisTemplate` + `ObjectMapper`
- Cập nhật `application/service/EventService.java`
  - Inject `RedisCacheStrategy<EventResponse> cacheStrategy`
  - `findById()`: dùng `cacheStrategy.getOrLoad(EVENT_CACHE + id, 30min, loader)`
  - `updateEvent()`, `updateStatus()`, `cancelEvent()`: thêm `cacheStrategy.evict(EVENT_CACHE + id)` sau khi save
- Cập nhật `service/EventServiceTest.java`
  - Thêm `@Mock RedisCacheStrategy<EventResponse> cacheStrategy`
  - Mock `getOrLoad` pass-through cho test `shouldThrowWhenEventNotFound`
  - Mock `evict` → `Mono.empty()` cho test transition to ON_SALE

**Task 14: Full Build Verification**
- `mvn test -pl event-catalog-service`: **15/15 tests PASS**
  - `EventControllerTest`: 3 tests
  - `EventServiceTest`: 5 tests
  - `LocationServiceTest`: 2 tests
  - `SeatServiceTest`: 3 tests
  - `VenueServiceTest`: 2 tests
- `mvn package -pl event-catalog-service -am -DskipTests`: **BUILD SUCCESS**
  - JAR: `event-catalog-service-1.0.0-SNAPSHOT.jar`
- Cập nhật `PLANNING.md`: `event-catalog-service` → 🟢 Done

### Git
- Commit: `feat(event-catalog): add Kafka consumers — TicketStatus + MediaUploaded skeleton`
- Commit: `feat(event-catalog): add Redis cache for event detail and EventPublisher bean`
- Commit: `feat(event-catalog): complete event-catalog-service implementation`
- Push branch `feature/event-catalog-service` lên remote `origin`

---

## Bugs fixed
- `EventServiceTest` fail sau khi thêm `cacheStrategy` vào `EventService`: cần thêm `@Mock RedisCacheStrategy<EventResponse>` và mock `getOrLoad` pass-through để test `findById` không bị NPE

---

## Đang làm dở
- PR `feature/event-catalog-service` chờ review/merge (gh CLI không cài — tạo thủ công trên GitHub)
- Next: `booking-service` (port 8083) — functional routing, seat locking, booking lifecycle
