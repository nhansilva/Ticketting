# Worklog — 2026-04-12

## Tổng kết ngày
Chạy thành công user-service, build ticketing-client (React + Vite), implement Google OAuth2.

---

## Actions thực hiện

### User Service — Fix & Run
- Fix Kafka deserializer typo: `deserialization` → `serialization`
- Fix R2DBC duplicate `@EnableR2dbcRepositories` (UserServiceApplication + DatabaseConfig)
- Exclude `RedisRepositoriesAutoConfiguration` để tránh Redis scan nhầm R2DBC repo
- Fix Redis port: `32769` → `6379`
- Fix DB credentials: `postgres/postgres` → `ticketing/ticketing123`, DB `ticketing` → `user_db`
- Fix Mongo Express port conflict: `8081` → `8091` (conflict với user-service)
- Tạo `user_db` thủ công trong IntelliJ Database tool
- Copy migration → `schema.sql`, config `spring.sql.init`
- Fix R2DBC INSERT/UPDATE: implement `Persistable<UUID>` trên `User`, `UserPreferences`, `VerificationToken`
- Thêm `.isNew(true)` vào tất cả chỗ build entity mới trong `UserServiceImpl`
- Fix phone validation regex: `^\\+?[1-9]\\d{1,14}$` → `^(\\+?84|0)[3-9]\\d{8}$` (hỗ trợ số VN)
- Fix login: đổi `findActiveUserByEmail` → `findByEmail` + filter status thủ công (cho phép `PENDING_VERIFICATION` login)

### CORS Fix
- Lần 1: Thêm `CorsConfigurationSource` vào SecurityConfig
- Lần 2: Dùng `CorsWebFilter` bean + `@Order(HIGHEST_PRECEDENCE)`
- Lần 3 (fix đúng): Dùng `setAllowedOriginPatterns(*)` thay vì `setAllowedOrigins(*)` — `allowCredentials=true` không compatible với wildcard origins

### ticketing-client — Setup
- Tạo project React + Vite + TypeScript tại `C:\Users\vtnha\Working\Architect\ticketing-client`
- Cài dependencies: react-router-dom, axios, react-hook-form, zod, @hookform/resolvers, zustand, @tanstack/react-query
- Cấu trúc: `api/`, `store/`, `types/`, `pages/auth/`, `pages/home/`, `router/`

### ticketing-client — Auth Pages
- `LoginPage` — form login với validation Zod
- `RegisterPage` — form đăng ký, success state
- `ForgotPasswordPage` — nhập email, gửi link reset
- `ResetPasswordPage` — đặt mật khẩu mới từ token URL
- `AuthPage.module.css` — dark theme glassmorphism UI

### ticketing-client — Home Page
- `HomePage` — navbar với search, hero banner, tab Concert/Phim, grid cards
- Mock data: 4 concert + 4 phim
- Filter realtime theo tên/artist/genre
- Tag HOT/MỚI/SOLD OUT, nút đặt vé

### ticketing-client — Routing
- `PrivateRoute` — redirect `/login` nếu chưa auth
- `PublicRoute` — redirect `/home` nếu đã auth
- Routes: `/login`, `/register`, `/forgot-password`, `/reset-password`, `/home`, `/auth/callback`

### Google OAuth2 — Planning & Implementation
- Thêm `spring-boot-starter-oauth2-client` dependency
- `GoogleOAuth2UserService` — upsert user từ Google profile vào DB
- `GoogleOAuth2User` — wrapper OAuth2User + User entity
- `OAuth2SuccessHandler` — tạo JWT → redirect `localhost:5173/auth/callback?token=xxx`
- `OAuth2FailureHandler` — redirect FE với `?error=oauth2_failed`
- Update `SecurityConfig` với `.oauth2Login()`
- Update `User` entity: thêm `googleId`, `authProvider (LOCAL|GOOGLE)`
- Update `schema.sql`: thêm `google_id`, `auth_provider`, `password_hash` nullable
- `CallbackPage` — nhận token từ URL, fetch profile, lưu store, redirect `/home`
- Nút "Login with Google" trên `LoginPage` + Google SVG icon
- Divider "hoặc" giữa Google button và form

### Documentation
- Update `PLANNING.md`: thêm section 1.2.1 Google OAuth2, update trạng thái services
- Tạo `LESSONS_LEARNED.md`: 7 lesson learned từ 2 ngày
- Update `CLAUDE.md`: thêm rule #7 ghi lesson learned sau mỗi lần fix
- Tạo `worklog/` directory + worklog theo ngày

---

## Bugs fixed hôm nay
1. Kafka deserializer ClassNotFoundException
2. Spring Data Redis scan nhầm R2DBC repo
3. Duplicate @EnableR2dbcRepositories
4. CORS 403 preflight — allowedOriginPatterns fix
5. Phone number VN bị reject
6. R2DBC UPDATE thay vì INSERT (Persistable fix)
7. Schema bảng chưa tạo
8. user_db chưa tồn tại
9. Mongo Express chiếm port 8081
10. Login reject PENDING_VERIFICATION user

---

## Đang làm dở
- Google OAuth2: cần điền `GOOGLE_CLIENT_ID` + `GOOGLE_CLIENT_SECRET` thật vào config
- Drop & recreate bảng `users` để có column `google_id`, `auth_provider`
